<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.size %></td>
  <td><%= button_to t('routes.start'), start_test_path(test), class: 'btn btn-primary' %></td>
</tr>