<h1><%=	t('.header', title: @test.title) %></h1>
<h1><%=	t('.level', level: @test.level) %></h1>

<table>
  <tr>
    <th><%= t('.question') %></th>
    <th colspan="3"><%= t('.actions')%></th>
  </tr>

  <%= render @test.questions %>

</table>
<%= link_to t('actions.edit'), edit_admin_test_path(@test) %> |
<%= link_to t('.new_question'), new_admin_test_question_path(@test.id) %> |
<%= link_to t('routes.back'), admin_tests_path %>