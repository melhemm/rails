<%= form_with model: [:admin, @badge], local: true do |form| %>
    <%= render 'shared/errors', resource: @badge %>
    <p>
      <%= form.label :title %><br>
      <%= form.text_field :title, id: :badge_title %>
    </p>
    <p>
      <%= form.label :img_url %><br>
      <%= form.text_field :img_url, id: :badge_img_url %>
    </p>
      <div class="form-group switch-field">
        <%= form.label :reward_rule %>
        <%= form.select :reward_rule, options_for_select(Badge::BADGE_RULES) %>

        <%= form.label :value %>
        <%= form.text_field :value %>
      </div>
    <p><%= form.submit %></p>
<% end %>
